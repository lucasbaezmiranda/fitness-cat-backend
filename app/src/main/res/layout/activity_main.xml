<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="32dp" />

    <!-- Stage row: Image on left, buttons on right -->
    <ImageView
        android:id="@+id/stageImageView"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="32dp"
        android:contentDescription="Stage image"
        android:scaleType="fitCenter"
        android:src="@drawable/stage_1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titleText" />

    <!-- Stage buttons container (vertical) -->
    <LinearLayout
        android:id="@+id/stageButtonsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toEndOf="@+id/stageImageView"
        app:layout_constraintTop_toTopOf="@+id/stageImageView"
        app:layout_constraintBottom_toBottomOf="@+id/stageImageView">

        <Button
            android:id="@+id/prevStageButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="◀"
            android:textSize="20sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/nextStageButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="▶"
            android:textSize="20sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

    </LinearLayout>

    <!-- Health bar row: Progress bar on left, buttons on right -->
    <ProgressBar
        android:id="@+id/healthProgressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="120dp"
        android:layout_height="32dp"
        android:layout_marginTop="16dp"
        android:max="100"
        android:progress="100"
        android:progressTint="#4CAF50"
        app:layout_constraintStart_toStartOf="@+id/stageImageView"
        app:layout_constraintTop_toBottomOf="@+id/stageImageView" />

    <!-- Health value text -->
    <TextView
        android:id="@+id/healthValueText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="100"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginTop="4dp"
        app:layout_constraintStart_toStartOf="@+id/healthProgressBar"
        app:layout_constraintTop_toBottomOf="@+id/healthProgressBar" />

    <!-- Health buttons container (vertical) -->
    <LinearLayout
        android:id="@+id/healthButtonsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toEndOf="@+id/healthProgressBar"
        app:layout_constraintTop_toTopOf="@+id/healthProgressBar"
        app:layout_constraintBottom_toBottomOf="@+id/healthValueText">

        <Button
            android:id="@+id/increaseHealthButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="▲"
            android:textSize="20sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/decreaseHealthButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="▼"
            android:textSize="20sp"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

    </LinearLayout>

    <TextView
        android:id="@+id/stepsLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pasos de hoy"
        android:textSize="20sp"
        android:layout_marginTop="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/healthValueText" />

    <TextView
        android:id="@+id/stepsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="48sp"
        android:textStyle="bold"
        android:textColor="#2196F3"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/stepsLabel" />

    <Button
        android:id="@+id/syncButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sincronizar ahora"
        android:layout_marginTop="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/stepsText" />

    <TextView
        android:id="@+id/debugStatusText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Estado: Esperando..."
        android:textSize="12sp"
        android:layout_marginTop="32dp"
        android:fontFamily="monospace"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/syncButton" />

</androidx.constraintlayout.widget.ConstraintLayout>
